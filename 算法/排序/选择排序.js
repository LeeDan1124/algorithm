// 选择排序
/**
 * 假设[0,nextSort)范围内为已经排好序的元素，选择排序的思想是,
 * 在[nextSort,n]范围内搜索最小的值arr[min]，
 * 并将arr[min]与arr[nextSort]进行交换，以此类推，直到所有的元素均被排好序。
 */
function quickSort(arr) {
  
}

let arr = [2,1];
console.log(quickSort(arr));
