class Graph {
    // 添加一条边（带权重）
    addEdge(from, to, weight) {}

    // 删除一条边
    removeEdge(from, to) {}

    // 判断两个节点是否相邻
    hasEdge(from, to) {}

    // 返回一条边的权重
    weight(from, to) {}

    // 返回某个节点的所有邻居节点和对应权重
    neighbors(v) {}

    // 返回节点总数
    size() {}
}
