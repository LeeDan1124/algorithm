/**
 * 提供一个方法实现在线文件内容的处理。如果是相同文件，不需要重复压缩。
已知

文件内容可以通过远程http获取或者本地读取，不限方法。
需要设计缓存机制
已经有具体的同步处理函数FileCompress，调用方法如下
 */
const fileCompress = (filename)=>{
  // some logic 已有
}
 
const result = fileCompress(filename);